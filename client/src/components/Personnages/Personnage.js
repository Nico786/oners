/* import {Col} from 'react-bootstrap';

import React from 'react';

const Personnage = ({avatarSrc, imgx2, imgx3}) => {
    return (
        <Col xs={12} lg={4}>
            <img src={avatarSrc} srcSet={`${imgx2} 2x,${imgx3} 3x`} />
        </Col>
    )
}

export default Personnage; */

